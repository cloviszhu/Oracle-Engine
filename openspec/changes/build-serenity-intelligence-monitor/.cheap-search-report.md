# Cheap Search 代码情报报告

扫描范围：`proposal.md`、`design.md`，以及现有 `client/`、`server/`、`shared/`、`drizzle/`、`scripts/` 和工程配置。以下只记录当前代码事实；“未找到”表示现有源码中没有对应业务实现。

## AC-1 真实 X API 获取及审计

- 相关文件（按相关性）：`.env.example:5`；`docker-compose.yml:51`；`drizzle/schema.ts:1-5`；`server/app.module.ts:6-13`。
- 代码事实：环境模板和 Compose 仅预留 `X_API_BEARER_TOKEN`；Nest 模块只注册静态资源与 `HealthController`；Drizzle schema 是空对象。
- 关键代码片段（`.env.example:5`）：
  ```dotenv
  X_API_BEARER_TOKEN=
  ```
- 关键代码片段（`drizzle/schema.ts:5`）：
  ```ts
  export const schema = {};
  ```
- 已有同类功能：仅有服务端环境变量边界的配置占位。
- 未找到：X API 客户端、轮询/游标、原始响应归档、来源审计或内容表。

## AC-2 回复、引用和历史上下文

- 相关文件（按相关性）：`drizzle/schema.ts:1-5`；`server/app.module.ts:6-13`；`shared/system.ts:1`。
- 代码事实：schema 注释明确业务表留待本 change 定义；当前后端没有内容或上下文模块；共享层只有系统名称常量。
- 关键代码片段（`drizzle/schema.ts:1-5`）：
  ```ts
  /**
   * 业务表由 build-serenity-intelligence-monitor change 定义。
   * 初始化阶段不猜测帖子、研究卡片或通知的数据模型。
   */
  export const schema = {};
  ```
- 已有同类功能：未找到。
- 未找到：回复对象、引用内容、历史观点关系、缺失上下文状态或上下文构造逻辑。

## AC-3 端到端幂等

- 相关文件（按相关性）：`drizzle/schema.ts:1-5`；`package.json:25-30`；`server/infrastructure/queue/queue.config.ts:1-9`；`server/infrastructure/database/database.config.ts:1-9`。
- 代码事实：依赖中已有 BullMQ、Drizzle、ioredis 和 mysql2；现有代码只读取并校验数据库/Redis URL；schema 无表和唯一约束。
- 关键代码片段（`server/infrastructure/queue/queue.config.ts:1-8`）：
  ```ts
  export function readRedisUrl(): string {
    const redisUrl = process.env.REDIS_URL?.trim();
    if (!redisUrl) {
      throw new Error('REDIS_URL is required');
    }
    return redisUrl;
  }
  ```
- 关键代码片段（`drizzle/schema.ts:5`）：
  ```ts
  export const schema = {};
  ```
- 已有同类功能：数据库与队列连接参数的失败可见读取。
- 未找到：内容、卡片或通知幂等键，游标，去重约束，作业稳定键或重启幂等处理。

## AC-4 编辑、删除和不可访问

- 相关文件（按相关性）：`drizzle/schema.ts:1-5`；`server/app.module.ts:6-13`。
- 代码事实：当前无业务状态字段、历史表或生命周期服务；Nest 模块没有内容控制器/服务。
- 关键代码片段（`server/app.module.ts:6-13`）：
  ```ts
  @Module({
    imports: [
      ServeStaticModule.forRoot({ rootPath: join(process.cwd(), 'dist', 'public') }),
    ],
    controllers: [HealthController],
  })
  export class AppModule {}
  ```
- 已有同类功能：未找到。
- 未找到：编辑版本、删除/不可访问状态机、tombstone、正文隐藏或安全重处理。

## AC-5 结构化中文研究卡片

- 相关文件（按相关性）：`drizzle/schema.ts:1-5`；`shared/system.ts:1`；`client/src/App.tsx:4-17`；`.env.example:6`。
- 代码事实：共享层没有研究卡片契约；前端只渲染工程初始化占位卡；环境模板仅预留 `AI_API_KEY`；数据库无卡片表。
- 关键代码片段（`client/src/App.tsx:8-13`）：
  ```tsx
  <p className="eyebrow">Private research workspace</p>
  <h1 id="system-title">{SYSTEM_NAME}</h1>
  <p className="status">工程初始化完成</p>
  <p className="description">
    当前仅包含可运行、可测试和可构建的工程骨架。业务功能将在 OpenSpec change 中定义和实现。
  </p>
  ```
- 关键代码片段（`.env.example:6`）：
  ```dotenv
  AI_API_KEY=
  ```
- 已有同类功能：现有 React 页面具有基础卡片式占位结构，但不承载研究数据。
- 未找到：AI 适配器、结构化输出 schema、翻译/判断/证据/不确定性字段或卡片持久化。

## AC-6 信息质量人工抽查

- 相关文件（按相关性）：`client/src/App.tsx:4-17`；`client/src/App.test.tsx:5-10`；`shared/system.ts:1`。
- 代码事实：当前页面和测试只验证系统名与“工程初始化完成”；没有研究详情展示。
- 关键代码片段（`client/src/App.test.tsx:5-10`）：
  ```tsx
  describe('App', () => {
    it('labels the site as an initialized research system', () => {
      const html = renderToStaticMarkup(<App />);
      expect(html).toContain('Serenity 海外产业信息监控');
      expect(html).toContain('工程初始化完成');
    });
  });
  ```
- 已有同类功能：React 静态渲染测试基线。
- 未找到：原文/翻译/作者判断/AI 解释分层、外链读取声明、A 股实体验证标记或观点变化证据展示。

## AC-7 可解释重要性

- 相关文件（按相关性）：`drizzle/schema.ts:1-5`；`server/app.module.ts:6-13`；`package.json:35`。
- 代码事实：项目依赖 Zod，但源码中尚无重要性规则或配置 schema；后端只注册健康检查。
- 关键代码片段（`package.json:31-35`）：
  ```json
  "react": "^19.2.1",
  "react-dom": "^19.2.1",
  "reflect-metadata": "^0.2.2",
  "rxjs": "^7.8.2",
  "zod": "^4.1.12"
  ```
- 关键代码片段（`server/app.module.ts:12-13`）：
  ```ts
  controllers: [HealthController],
  })
  ```
- 已有同类功能：未找到。
- 未找到：分项评分、权重、阈值、解释、归档/通知决策或模型不可覆盖规则。

## AC-8 通知恢复与去重

- 相关文件（按相关性）：`.env.example:7`；`docker-compose.yml:53`；`server/infrastructure/queue/queue.config.ts:1-9`；`package.json:25-29`；`drizzle/schema.ts:1-5`。
- 代码事实：仅预留飞书 Webhook 和 Redis URL；BullMQ/ioredis 已安装但没有队列实例、worker 或通知表。
- 关键代码片段（`.env.example:3,7`）：
  ```dotenv
  REDIS_URL=redis://localhost:6379
  FEISHU_WEBHOOK_URL=
  ```
- 关键代码片段（`package.json:25-29`）：
  ```json
  "bullmq": "^5.76.8",
  "dotenv": "^17.4.2",
  "drizzle-orm": "^0.45.2",
  "express": "^5.1.0",
  "ioredis": "^5.10.1"
  ```
- 已有同类功能：Redis 配置缺失会抛出明确错误。
- 未找到：通知稳定去重键、发送状态、失败原因、超时、重试或 delivery 保留逻辑。

## AC-9 真实飞书提醒

- 相关文件（按相关性）：`.env.example:7`；`docker-compose.yml:53`；`scripts/audit-secrets.mjs:20-24`；`server/app.module.ts:6-13`。
- 代码事实：飞书 Webhook 只作为服务端环境变量预留；Secret 扫描包含飞书机器人 URL 模式；后端没有通知控制器或适配器。
- 关键代码片段（`scripts/audit-secrets.mjs:20-24`）：
  ```js
  const secretPatterns = [
    /Bearer\s+[A-Za-z0-9._-]{20,}/,
    /https:\/\/open\.feishu\.cn\/open-apis\/bot\/v2\/hook\/[A-Za-z0-9-]{20,}/,
    /(?:api[_-]?key|access[_-]?token|client[_-]?secret)\s*[:=]\s*["'][^"']{12,}["']/i,
  ];
  ```
- 关键代码片段（`.env.example:7`）：
  ```dotenv
  FEISHU_WEBHOOK_URL=
  ```
- 已有同类功能：仓库扫描可识别形如飞书机器人 Webhook 的泄露文本。
- 未找到：飞书发送、签名、限流、返回值记录、私有详情链接或“待配置”运行状态。

## AC-10 私有认证、浏览、搜索和筛选

- 相关文件（按相关性）：`.env.example:4`；`docker-compose.yml:50`；`client/src/App.tsx:4-17`；`server/app.module.ts:6-13`；`server/health/health.controller.ts:8-16`。
- 代码事实：`SESSION_SECRET` 已预留且 Compose 启动时要求非空；前端只有单个占位页面；后端唯一 API 是公开的 `GET /api/health`。
- 关键代码片段（`docker-compose.yml:46-53`）：
  ```yaml
  environment:
    PORT: 3000
    DATABASE_URL: mysql://serenity:serenity@db:3306/serenity
    REDIS_URL: redis://redis:6379
    SESSION_SECRET: ${SESSION_SECRET:?SESSION_SECRET is required}
    X_API_BEARER_TOKEN: ${X_API_BEARER_TOKEN:-}
    AI_API_KEY: ${AI_API_KEY:-}
    FEISHU_WEBHOOK_URL: ${FEISHU_WEBHOOK_URL:-}
  ```
- 关键代码片段（`server/health/health.controller.ts:8-11`）：
  ```ts
  @Controller('api/health')
  export class HealthController {
    @Get()
    getHealth(): HealthResponse {
  ```
- 已有同类功能：服务端 Session Secret 配置边界占位、React/Nest 页面/API 骨架。
- 未找到：登录/登出/会话 API、认证守卫、情报列表/详情 API、时间线、分页、搜索或组合筛选。

## AC-11 用户反馈

- 相关文件（按相关性）：`drizzle/schema.ts:1-5`；`client/src/App.tsx:4-17`；`server/app.module.ts:6-13`。
- 代码事实：数据库无反馈表，前端无反馈控件，后端无反馈路由。
- 关键代码片段（`drizzle/schema.ts:5`）：
  ```ts
  export const schema = {};
  ```
- 关键代码片段（`server/app.module.ts:12-13`）：
  ```ts
  controllers: [HealthController],
  })
  ```
- 已有同类功能：未找到。
- 未找到：六种固定反馈、用户/卡片/时间追溯或追加式反馈记录。

## AC-12 系统运行状态

- 相关文件（按相关性）：`server/health/health.controller.ts:3-16`；`server/health/health.controller.test.ts:1-15`；`client/src/App.tsx:7-13`；`server/app.module.ts:12-13`。
- 代码事实：现有健康接口固定返回 `status: ok` 与服务名；现有前端只显示静态“工程初始化完成”；模块注册了健康控制器。
- 关键代码片段（`server/health/health.controller.ts:8-16`）：
  ```ts
  @Controller('api/health')
  export class HealthController {
    @Get()
    getHealth(): HealthResponse {
      return {
        status: 'ok',
        service: 'serenity-intelligence-monitor',
      };
    }
  ```
- 已有同类功能：基础健康检查及其单元测试。
- 未找到：获取/补偿/上下文/AI/通知的最近状态、失败原因、待配置项、任务摘要或恢复入口。

## AC-13 故障恢复

- 相关文件（按相关性）：`server/main.ts:6-11`；`server/infrastructure/queue/queue.config.ts:1-9`；`server/infrastructure/config.test.ts:22-41`；`package.json:25-29`。
- 代码事实：Nest 启用了 shutdown hooks；Redis URL 缺失会抛错并有测试；BullMQ/ioredis 已安装但未实例化。
- 关键代码片段（`server/main.ts:6-11`）：
  ```ts
  async function bootstrap(): Promise<void> {
    const app = await NestFactory.create(AppModule);
    app.enableShutdownHooks();
    const port = Number.parseInt(process.env.PORT ?? '3000', 10);
    await app.listen(port, '0.0.0.0');
  }
  ```
- 关键代码片段（`server/infrastructure/config.test.ts:33-40`）：
  ```ts
  it('rejects a missing Redis URL', () => {
    delete process.env.REDIS_URL;
    expect(() => readRedisUrl()).toThrowError('REDIS_URL is required');
  });
  it('returns the configured Redis URL', () => {
    process.env.REDIS_URL = 'redis://example:6379';
    expect(readRedisUrl()).toBe('redis://example:6379');
  });
  ```
- 已有同类功能：进程关闭钩子和基础设施配置失败可见。
- 未找到：lease、attempt、reconciliation、退避、终态/死信、限流恢复或服务重启续办。

## AC-14 Prompt Injection 防护

- 相关文件（按相关性）：`.env.example:6`；`docker-compose.yml:52`；`server/app.module.ts:6-13`；`package.json:35`。
- 代码事实：AI Key 仅有配置占位；Zod 已安装；当前没有模型调用或不可信内容进入模型的代码路径。
- 关键代码片段（`.env.example:6`）：
  ```dotenv
  AI_API_KEY=
  ```
- 关键代码片段（`package.json:35`）：
  ```json
  "zod": "^4.1.12"
  ```
- 已有同类功能：未找到。
- 未找到：不可信数据 envelope、系统指令隔离、无工具模型边界、严格输出校验或恶意 fixture 处理。

## AC-15 Secret 隔离

- 相关文件（按相关性）：`scripts/audit-secrets.mjs:4-24`；`scripts/audit-secrets.mjs:26-63`；`package.json:16`；`.env.example:1-7`；`docker-compose.yml:46-53`。
- 代码事实：扫描脚本递归检查文本文件，排除 `.git`、产物与依赖目录，匹配 Bearer、飞书 Webhook、API key/token/secret 赋值；发现后设置失败退出码；`pnpm audit:secrets` 已接入；Secret 均在服务端环境变量边界预留。
- 关键代码片段（`scripts/audit-secrets.mjs:45-60`）：
  ```js
  const findings = [];
  for (const file of collectFiles(root)) {
    const content = readFileSync(file, 'utf8');
    for (const pattern of secretPatterns) {
      if (pattern.test(content)) {
        findings.push(`${relative(root, file)} matched ${pattern.source}`);
      }
    }
  }
  if (findings.length > 0) {
    console.error('Potential secrets detected:');
    for (const finding of findings) console.error(`- ${finding}`);
    process.exitCode = 1;
  }
  ```
- 关键代码片段（`package.json:16`）：
  ```json
  "audit:secrets": "node scripts/audit-secrets.mjs"
  ```
- 已有同类功能：仓库级静态 Secret 扫描和 Compose 必填 `SESSION_SECRET`。
- 未找到：日志集中脱敏、API/错误页 DTO 防泄露、前端 bundle/测试快照专项断言或运行时配置对象。

## AC-16 只读研究边界

- 相关文件（按相关性）：`package.json:20-35`；`server/app.module.ts:6-13`；`drizzle/schema.ts:1-5`；`client/src/App.tsx:8-13`。
- 代码事实：运行依赖集中于 Web、数据库、队列与校验；当前唯一后端能力是健康检查，数据库无业务表，页面自称私有研究工作区骨架。
- 关键代码片段（`server/app.module.ts:6-13`）：
  ```ts
  @Module({
    imports: [
      ServeStaticModule.forRoot({ rootPath: join(process.cwd(), 'dist', 'public') }),
    ],
    controllers: [HealthController],
  })
  export class AppModule {}
  ```
- 关键代码片段（`drizzle/schema.ts:5`）：
  ```ts
  export const schema = {};
  ```
- 已有同类功能：当前代码未提供任何交易动作。
- 未找到：券商、账户、持仓、行情、交易、Cookie 抓取、模拟登录或 Hook 接口；也未找到显式越界输入拒绝策略。

## AC-17 版本和成本控制

- 相关文件（按相关性）：`.env.example:5-6`；`docker-compose.yml:51-52`；`drizzle/schema.ts:1-5`；`server/app.module.ts:6-13`。
- 代码事实：X 和 AI 凭据只有环境变量占位；没有调用记录表或外部适配器。
- 关键代码片段（`.env.example:5-6`）：
  ```dotenv
  X_API_BEARER_TOKEN=
  AI_API_KEY=
  ```
- 关键代码片段（`drizzle/schema.ts:5`）：
  ```ts
  export const schema = {};
  ```
- 已有同类功能：未找到。
- 未找到：供应商/模型/API 版本、usage/cost、预算守卫、速率上限、停止新增调用或状态展示。

## AC-18 验收证据分层

- 相关文件（按相关性）：`package.json:11-18`；`client/src/App.test.tsx:5-10`；`server/infrastructure/config.test.ts:22-41`；`server/health/health.controller.test.ts:1-15`；`docker-compose.yml:1-59`。
- 代码事实：已有 check/test/lint/build/Secret 扫描命令与三个基础测试文件；Compose 定义 MySQL、Redis、app 和健康依赖；源码没有验收报告生成或证据分类结构。
- 关键代码片段（`package.json:11-18`）：
  ```json
  "build": "vite build && nest build --path tsconfig.server.json",
  "start": "cross-env NODE_ENV=production node dist/server/main.js",
  "check": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.server.json",
  "test": "vitest run",
  "lint": "eslint client server shared drizzle vite.config.ts vitest.config.ts drizzle.config.ts",
  "audit:secrets": "node scripts/audit-secrets.mjs",
  "db:generate": "drizzle-kit generate",
  "db:push": "drizzle-kit push"
  ```
- 关键代码片段（`docker-compose.yml:41-45`）：
  ```yaml
  depends_on:
    db:
      condition: service_healthy
    redis:
      condition: service_healthy
  ```
- 已有同类功能：自动化测试、构建与 Secret 扫描的工程入口。
- 未找到：按“自动化 / Mock / 真实 API / 待人工”分类的结果模型、报告或外部凭据验证记录。

## 汇总事实

- 当前业务实现覆盖：AC-1～AC-18 均未形成业务闭环。
- 可直接识别的工程基线：NestJS 健康接口、React 占位页、MySQL/Redis URL 读取、Docker Compose 服务定义、Vitest 测试入口、仓库 Secret 扫描。
- 当前无内容、上下文、研究卡片、评分、通知、认证、反馈、作业恢复、外部 usage 或验收证据业务数据模型；`drizzle/schema.ts:5` 仍为 `schema = {}`。
